import DirectionComponent from "../../components/about/DirectionComponent";

const directionPage = () => {
    return (
        <div>
            <DirectionComponent/>
        </div>
    );
};

export default directionPage;